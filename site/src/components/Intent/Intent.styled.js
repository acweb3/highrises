import styled from 'styled-components';

export const TheDrake = styled.img`
    position: absolute;
    top: 0;
    right: 0;
    height: 100%;
    z-index: 3;
    max-width: initial;
`;

export const Intent = styled.div`
    position: relative;
`;
