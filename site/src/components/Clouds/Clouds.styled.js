import styled from 'styled-components';

export const Clouds = styled.div`
    position: absolute;
    top: 0;
    left: 0;
    height: 100%;
    width: 100%;

    overflow: hidden;

    background: rgba(0, 0, 0, 0.04);
`;
